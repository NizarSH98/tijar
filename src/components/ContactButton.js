// src/components/ContactButton.js

import React from 'react';
import '../styles/ContactButton.css'; // Ensure to create this CSS file

const ContactButton = () => {
  return (
    <div className="contact-button">
      <button>Contact Us</button>
    </div>
  );
};

export default ContactButton;
